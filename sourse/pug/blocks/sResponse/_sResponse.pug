mixin sResponse(heading)
	// start sResponse
	+b.SECTION.sResponse.section#sResponse&attributes(attributes)
		.container
			.card
				+e.heading
					p= heading
				if block
					block
	// end sResponse